#include "GridView.h"
namespace view {
    void view::GridView::draw(sf::RenderTarget& target, sf::RenderStates states) const
    {
    }

    sf::Vector2f view::GridView::getPosition() const
    {
        return {};
    }
}